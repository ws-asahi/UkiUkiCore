# Shell scripts must be checked out with LF endings even when
# core.autocrlf=true on Windows, or bash/WSL fails on the shebang with
#   env: 'bash\r': No such file or directory
*.sh text eol=lf
# PowerShell is CRLF-tolerant but keep it declared as text.
*.ps1 text
